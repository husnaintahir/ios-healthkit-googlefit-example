import { View } from 'react-native';
import React from 'react';


import { Sizing } from '../helpers/Sizing';

export const Border = ({  }) => {
    return (
        <View
            style={{
                height: Sizing(1), backgroundColor: "#CBD5E0"
            }}
        />
    );
}